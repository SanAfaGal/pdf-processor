# PDF Processor - Production Dependencies
# Pin versions for reproducible builds

# Data processing
pandas>=1.5.0,<2.0.0
numpy>=1.23.0,<2.0.0
openpyxl>=3.9.0  # Excel support for pandas

# PDF operations
pymupdf>=1.23.0  # PDF reading and validation
ocrmypdf>=14.0.0  # Optical character recognition

# Configuration management
pydantic>=2.0.0  # Settings validation and parsing
python-dotenv>=0.20.0  # Environment variable loading

# Utilities
pathlib2>=2.3.0; python_version < '3.4'  # For Python < 3.4 compatibility (usually not needed for 3.10+)

# Development and testing (optional)
pytest>=7.2.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.990
isort>=5.11.0
